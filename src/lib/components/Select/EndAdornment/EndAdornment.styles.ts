import { makeStyles } from '../../../../themes/core';

const useStyles = makeStyles({
  name: 'EndAdornment',
})(() => ({
  multipleEndAdornment: {
    flex: 1,
    justifyContent: 'flex-end',
  },
}));

export default useStyles;
